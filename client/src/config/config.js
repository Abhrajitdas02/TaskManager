const config = {
  API_URL:
    process.env.REACT_APP_API_URL || "https://taskmanager-9aik.onrender.com",
  SOCKET_URL:
    process.env.REACT_APP_SOCKET_URL || "https://taskmanager-9aik.onrender.com",
};

export default config;
